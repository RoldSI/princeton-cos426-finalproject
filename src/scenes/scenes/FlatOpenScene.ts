import BaseScene from "../Scene";

class FlatOpenScene extends BaseScene {
    resetState(): void {
        throw new Error("Method not implemented.");
    }
    update(_timeStamp: number): void {
        
    }
}